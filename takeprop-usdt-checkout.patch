From 4aa32888a7d57886cbf4a795187baa9d57d31c35 Mon Sep 17 00:00:00 2001
From: Codex <codex@openai.com>
Date: Mon, 17 Aug 2026 07:49:23 +0200
Subject: [PATCH] Add secure USDT checkout flow

---
 apps/web/app/api/orders/usdt/route.ts         | 41 +++++++++++++++++++
 apps/web/app/checkout/checkout-form.tsx       | 38 +++++++++++++++++
 apps/web/app/checkout/checkout.css            |  2 +-
 apps/web/app/checkout/network.css             |  3 ++
 apps/web/app/checkout/page.tsx                | 21 ++++++++--
 apps/web/app/dashboard/page.tsx               |  5 ++-
 apps/web/lib/plans.ts                         |  4 +-
 apps/web/lib/supabase-rest.ts                 | 13 ++++++
 ...0817090000_usdt_bep20_order_submission.sql | 28 +++++++++++++
 9 files changed, 148 insertions(+), 7 deletions(-)
 create mode 100644 apps/web/app/api/orders/usdt/route.ts
 create mode 100644 apps/web/app/checkout/checkout-form.tsx
 create mode 100644 apps/web/app/checkout/network.css
 create mode 100644 supabase/migrations/20260817090000_usdt_bep20_order_submission.sql

diff --git a/apps/web/app/api/orders/usdt/route.ts b/apps/web/app/api/orders/usdt/route.ts
new file mode 100644
index 0000000..b3ea490
--- /dev/null
+++ b/apps/web/app/api/orders/usdt/route.ts
@@ -0,0 +1,41 @@
+import { NextResponse } from 'next/server'
+import { getAuthenticatedUser, getOwnData, postOwnData } from '../../../../lib/supabase-rest'
+
+type Plan = { id: string; slug: string; name: string; account_size: number; price: number; currency: string; version: number; rules: Record<string, unknown> }
+const networks = {
+  bep20: { provider: 'usdt_bep20', wallet: '0x30127dee8f4bfeaec586c32d580a8b6066eac11b' },
+  erc20: { provider: 'usdt_erc20', wallet: '0x30127dee8f4bfeaec586c32d580a8b6066eac11b' },
+  trc20: { provider: 'usdt_trc20', wallet: 'TLTKYRdtpaaYXoSHMgUgEJ3BGARbQkcx3g' },
+  ton: { provider: 'usdt_ton', wallet: 'UQCuT9QECsZp-iBmSM1v8c8Gdga3JWww1sENWn6sywc4cjKc' }
+} as const
+
+export async function POST(request: Request) {
+  try {
+    const auth = await getAuthenticatedUser()
+    if (!auth) return NextResponse.json({ error: 'Please sign in before submitting a payment.' }, { status: 401 })
+    const { planId, txHash, network } = await request.json()
+    if (typeof planId !== 'string') return NextResponse.json({ error: 'Choose a valid challenge plan.' }, { status: 400 })
+    const selected = typeof network === 'string' ? networks[network as keyof typeof networks] : undefined
+    if (!selected) return NextResponse.json({ error: 'Choose a supported USDT network.' }, { status: 400 })
+    if (typeof txHash !== 'string' || !/^[A-Za-z0-9+/_=-]{20,100}$/.test(txHash.trim())) return NextResponse.json({ error: 'Enter a valid transaction hash for the selected network.' }, { status: 400 })
+    const plans = await getOwnData<Plan[]>(`challenge_plans?select=id,slug,name,account_size,price,currency,version,rules&id=eq.${encodeURIComponent(planId)}&is_active=eq.true&limit=1`, auth.accessToken)
+    const plan = plans[0]
+    if (!plan) return NextResponse.json({ error: 'This challenge plan is unavailable.' }, { status: 404 })
+    const orders = await postOwnData<{ id: string }[]>('orders', auth.accessToken, {
+      user_id: auth.user.id,
+      plan_id: plan.id,
+      amount: plan.price,
+      currency: 'USDT',
+      status: 'pending',
+      payment_provider: selected.provider,
+      provider_checkout_id: selected.wallet,
+      provider_payment_id: txHash.trim().toLowerCase(),
+      plan_version: plan.version,
+      plan_snapshot: { slug: plan.slug, name: plan.name, account_size: plan.account_size, price: plan.price, currency: plan.currency, rules: plan.rules }
+    })
+    return NextResponse.json({ ok: true, orderId: orders[0]?.id })
+  } catch (error) {
+    const message = error instanceof Error && error.message.includes('duplicate') ? 'This transaction hash has already been submitted.' : 'Unable to submit payment. Check the transaction hash and try again.'
+    return NextResponse.json({ error: message }, { status: 400 })
+  }
+}
diff --git a/apps/web/app/checkout/checkout-form.tsx b/apps/web/app/checkout/checkout-form.tsx
new file mode 100644
index 0000000..a6e24cf
--- /dev/null
+++ b/apps/web/app/checkout/checkout-form.tsx
@@ -0,0 +1,38 @@
+'use client'
+
+import { FormEvent, useState } from 'react'
+
+const networks = {
+  bep20: { label: 'BNB Smart Chain (BEP20)', short: 'BEP20', address: '0x30127dee8f4bfeaec586c32d580a8b6066eac11b', warning: 'Only send USDT using BNB Smart Chain (BEP20).' },
+  erc20: { label: 'Ethereum (ERC20)', short: 'ERC20', address: '0x30127dee8f4bfeaec586c32d580a8b6066eac11b', warning: 'Only send USDT using Ethereum (ERC20). Network fees may be higher.' },
+  trc20: { label: 'Tron (TRC20)', short: 'TRC20', address: 'TLTKYRdtpaaYXoSHMgUgEJ3BGARbQkcx3g', warning: 'Only send USDT using Tron (TRC20).' },
+  ton: { label: 'TON', short: 'TON', address: 'UQCuT9QECsZp-iBmSM1v8c8Gdga3JWww1sENWn6sywc4cjKc', warning: 'Only send USDT on TON. Do not send NFTs or another token.' }
+} as const
+type Network = keyof typeof networks
+
+export default function CheckoutForm({ planId }: { planId: string }) {
+  const [message, setMessage] = useState('')
+  const [loading, setLoading] = useState(false)
+  const [copied, setCopied] = useState(false)
+  const [network, setNetwork] = useState<Network>('bep20')
+  const selected = networks[network]
+  async function copyAddress() {
+    await navigator.clipboard.writeText(selected.address)
+    setCopied(true); setTimeout(() => setCopied(false), 1800)
+  }
+  async function submit(event: FormEvent<HTMLFormElement>) {
+    event.preventDefault(); setLoading(true); setMessage('')
+    const form = new FormData(event.currentTarget)
+    const response = await fetch('/api/orders/usdt', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ planId, txHash: form.get('txHash'), network }) })
+    const result = await response.json(); setLoading(false)
+    if (!response.ok) return setMessage(result.error || 'Unable to submit payment.')
+    window.location.href = '/dashboard?payment=submitted'
+  }
+  return <div className="paymentBox">
+    <div className="network"><span>PAYMENT NETWORK</span><div className="networkTabs">{(Object.keys(networks) as Network[]).map(key=><button type="button" key={key} className={network===key?'on':''} onClick={()=>{setNetwork(key);setCopied(false);setMessage('')}}>{networks[key].short}</button>)}</div><b>USDT · {selected.label}</b><small>{selected.warning} Funds sent using another network may be lost.</small></div>
+    <div className="wallet"><span>DEPOSIT ADDRESS</span><code>{selected.address}</code><button type="button" onClick={copyAddress}>{copied ? 'Copied ✓' : 'Copy address'}</button></div>
+    <ol><li>Send the exact USDT amount shown above.</li><li>Wait for the transaction to appear in your wallet history.</li><li>Paste the BEP20 transaction hash below.</li></ol>
+    <form onSubmit={submit}><label>TRANSACTION HASH (TXID)<input name="txHash" required minLength={20} maxLength={100} placeholder="Paste the transaction hash" autoComplete="off" /></label>{message && <div className="checkoutMessage">{message}</div>}<button className="btn" disabled={loading}>{loading ? 'Submitting…' : 'I have paid — submit for review →'}</button></form>
+    <small className="reviewNote">Payments are reviewed before a simulated account is issued. Never send funds from an unsupported network.</small>
+  </div>
+}
diff --git a/apps/web/app/checkout/checkout.css b/apps/web/app/checkout/checkout.css
index 1c1b93b..b8bb6ad 100644
--- a/apps/web/app/checkout/checkout.css
+++ b/apps/web/app/checkout/checkout.css
@@ -1 +1 @@
-.checkout{min-height:100vh;display:grid;place-items:center;background:radial-gradient(circle at 50% 20%,#15352555,transparent 35%)}.checkout>div{width:min(550px,calc(100% - 30px));padding:35px}.checkout section{margin-top:45px}.checkout section>span{font-size:9px;color:var(--green);letter-spacing:2px}.checkout h1{font-size:42px;letter-spacing:-2px;margin:12px 0}.checkout section>p{color:#7d8b87}.summary{background:var(--panel);border:1px solid var(--line);margin:25px 0}.summary header,.summary>div{display:flex;justify-content:space-between;padding:16px 20px;border-bottom:1px solid var(--line)}.summary header div{display:flex;flex-direction:column}.summary header small,.summary>div span{color:#6e7d78;font-size:9px}.summary header div>b{font-size:23px}.summary header>strong{font-size:25px;color:var(--green)}.summary>div b{font-size:11px}.checkout label{display:flex;gap:10px;color:#74827d;font-size:10px}.checkout section>.btn{width:100%;border:0;margin:22px 0}.checkout section>a:last-child{display:block;text-align:center;color:#72817c;font-size:11px}
+.checkoutPage{min-height:100vh;background:radial-gradient(circle at 15% 45%,#43e28a13,transparent 30%),var(--bg)}.checkoutPage>header{height:84px;width:min(1120px,calc(100% - 40px));margin:auto;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line)}.checkoutPage>header>a:last-child{color:#7e8c87;font-size:12px}.checkoutPage>section{width:min(1050px,calc(100% - 40px));margin:auto;padding:75px 0 110px;display:grid;grid-template-columns:.9fr 1.1fr;gap:70px}.checkoutIntro>span,.paymentBox span{font-size:9px;letter-spacing:1.8px;color:#72b992;font-weight:800}.checkoutIntro h1{font-size:48px;line-height:1.04;letter-spacing:-2.5px;margin:18px 0}.checkoutIntro>p{color:#83918d;line-height:1.7}.orderCard{margin-top:35px;background:#0b1317;border:1px solid var(--line);padding:28px}.orderCard>small{color:#61706b;font-size:8px;letter-spacing:1.5px}.orderCard h2{font-size:38px;margin:12px 0 2px}.orderCard>p{color:#6f7d79;font-size:11px}.orderCard>div{display:grid;grid-template-columns:1fr 1fr;margin:24px 0;border-top:1px solid var(--line)}.orderCard>div span{display:flex;flex-direction:column;padding:14px 0;border-bottom:1px solid var(--line);color:#65736e}.orderCard>div b{color:#e3eae7;margin-top:5px;font-size:12px}.orderCard>strong{font-size:25px;color:var(--green)}.paymentBox{background:#0a1216;border:1px solid #26373d;padding:32px}.network{border:1px solid #614d1f;background:#211c0e;padding:18px}.network b,.network small{display:block}.network b{margin:8px 0;color:#f0d477}.network small{color:#a49366;line-height:1.5;font-size:10px}.wallet{margin:22px 0;padding:20px;background:#0d181d;border:1px solid var(--line)}.wallet code{display:block;color:#d9e6e0;word-break:break-all;font-size:12px;line-height:1.6;margin:10px 0}.wallet button{border:1px solid #315f48;background:#10251a;color:var(--green);padding:8px 12px;cursor:pointer}.paymentBox ol{color:#798782;font-size:11px;line-height:2;padding-left:20px}.paymentBox form{display:grid;gap:15px;margin-top:22px}.paymentBox label{font-size:8px;letter-spacing:1.4px;color:#6e7d78}.paymentBox input{display:block;width:100%;margin-top:8px;border:1px solid #26353a;background:#081015;color:#f3f7f5;padding:15px;border-radius:3px;outline:none}.paymentBox form>.btn{border:0;cursor:pointer}.paymentBox form>.btn:disabled{opacity:.6}.checkoutMessage{border:1px solid #715b2a;background:#241f10;color:#e5c76e;padding:12px;font-size:11px}.reviewNote{display:block;color:#596762;line-height:1.6;margin-top:18px;font-size:9px}@media(max-width:760px){.checkoutPage>section{grid-template-columns:1fr;padding-top:45px;gap:35px}.checkoutIntro h1{font-size:39px}.paymentBox{padding:22px}.orderCard{padding:22px}}
diff --git a/apps/web/app/checkout/network.css b/apps/web/app/checkout/network.css
new file mode 100644
index 0000000..16a8185
--- /dev/null
+++ b/apps/web/app/checkout/network.css
@@ -0,0 +1,3 @@
+.networkTabs{display:grid;grid-template-columns:repeat(4,1fr);gap:5px;margin:12px 0}
+.networkTabs button{border:1px solid #57491f;background:#14130c;color:#9b8a58;padding:9px 4px;cursor:pointer;font-size:10px}
+.networkTabs button.on{background:#4c401a;color:#ffe58a;border-color:#a78b36}
diff --git a/apps/web/app/checkout/page.tsx b/apps/web/app/checkout/page.tsx
index 5e67089..0d255da 100644
--- a/apps/web/app/checkout/page.tsx
+++ b/apps/web/app/checkout/page.tsx
@@ -1,3 +1,18 @@
-import Link from 'next/link';import {plans,money} from '../../lib/plans'
-export default function Checkout({searchParams}:{searchParams:{plan?:string}}){const plan=plans.find(p=>p.id===searchParams.plan)||plans[0];return <main className="checkout"><div><Link className="brand" href="/"><b>TP</b><strong>TakeProp</strong></Link><section><span>SECURE DEMO CHECKOUT</span><h1>Review your challenge.</h1><p>Confirm the evaluation rules. No real charge will be made in this MVP.</p><div className="summary"><header><div><small>{plan.stepType==='ONE_STEP'?'ONE-STEP':'TWO-STEP'} CHALLENGE</small><b>{money(plan.balance)}</b></div><strong>{money(plan.price)}</strong></header><Row a="Profit target" b={plan.targets.join('% + ')+'%'}/><Row a="Daily loss limit" b="3%"/><Row a="Static max loss" b="6%"/><Row a="Reward split" b="90%"/></div><label><input type="checkbox"/> I understand that all trading is simulated and rewards are subject to review and agreement.</label><button className="btn">Create demo order →</button><Link href="/dashboard">Already have an account? Log in</Link></section></div></main>}
-function Row({a,b}:{a:string,b:string}){return <div><span>{a}</span><b>{b}</b></div>}
+import Link from 'next/link'
+import { redirect } from 'next/navigation'
+import { getAuthenticatedUser, getOwnData } from '../../lib/supabase-rest'
+import CheckoutForm from './checkout-form'
+import './checkout.css'
+import './network.css'
+
+type Plan = { id: string; slug: string; name: string; account_size: number; price: number; profit_target_pct: number; daily_loss_limit_pct: number; max_loss_limit_pct: number; profit_split_pct: number; rules: { challenge_type?: string; phase_2_profit_target_pct?: number } }
+const usd=(value:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(value)
+
+export default async function Checkout({ searchParams }: { searchParams: { plan?: string } }) {
+  const auth = await getAuthenticatedUser(); if (!auth) redirect(`/auth?next=${encodeURIComponent(`/checkout?plan=${searchParams.plan || ''}`)}`)
+  const slug = typeof searchParams.plan === 'string' ? searchParams.plan : ''
+  const plans = await getOwnData<Plan[]>(`challenge_plans?select=id,slug,name,account_size,price,profit_target_pct,daily_loss_limit_pct,max_loss_limit_pct,profit_split_pct,rules&slug=eq.${encodeURIComponent(slug)}&is_active=eq.true&limit=1`, auth.accessToken)
+  const plan = plans[0]; if (!plan) redirect('/#plans')
+  const twoStep = plan.rules?.challenge_type === 'two_step'
+  return <main className="checkoutPage"><header><Link className="brand" href="/"><b>TP</b><strong>TakeProp</strong></Link><Link href="/#plans">← Change plan</Link></header><section><div className="checkoutIntro"><span>SECURE CHECKOUT</span><h1>Start your {twoStep ? 'Two-Step' : 'One-Step'} challenge.</h1><p>Review the fixed rules, pay with USDT on BEP20 and submit the blockchain transaction for verification.</p><div className="orderCard"><small>SELECTED CHALLENGE</small><h2>{usd(plan.account_size)}</h2><p>{plan.name}</p><div><span>Profit target<b>{twoStep ? `${plan.profit_target_pct}% / ${plan.rules.phase_2_profit_target_pct || 5}%` : `${plan.profit_target_pct}%`}</b></span><span>Daily loss<b>{plan.daily_loss_limit_pct}%</b></span><span>Static max loss<b>{plan.max_loss_limit_pct}%</b></span><span>Reward split<b>{plan.profit_split_pct}%</b></span></div><strong>{plan.price} USDT</strong></div></div><CheckoutForm planId={plan.id}/></section></main>
+}
diff --git a/apps/web/app/dashboard/page.tsx b/apps/web/app/dashboard/page.tsx
index 86e624e..ee4e13a 100644
--- a/apps/web/app/dashboard/page.tsx
+++ b/apps/web/app/dashboard/page.tsx
@@ -6,13 +6,16 @@ import LogoutButton from './logout-button'
 type Profile={full_name:string|null;kyc_status:string}
 type Plan={name:string;profit_target_pct:number;daily_loss_limit_pct:number;max_loss_limit_pct:number}
 type Account={display_account_id:string|null;status:string;phase:string;starting_balance:number;balance:number;equity:number;daily_pnl:number;total_pnl:number;challenge_plans:Plan|null}
+type Order={id:string;status:string;amount:number;currency:string;created_at:string;challenge_plans:{name:string;account_size:number}|null}
 const usd=(value:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(value)
 
 export default async function Dashboard(){
  const auth=await getAuthenticatedUser(); if(!auth) redirect('/auth')
  const profiles=await getOwnData<Profile[]>(`profiles?select=full_name,kyc_status&id=eq.${auth.user.id}&limit=1`,auth.accessToken)
  const accounts=await getOwnData<Account[]>(`trading_accounts?select=display_account_id,status,phase,starting_balance,balance,equity,daily_pnl,total_pnl,challenge_plans(name,profit_target_pct,daily_loss_limit_pct,max_loss_limit_pct)&user_id=eq.${auth.user.id}&order=created_at.desc&limit=1`,auth.accessToken)
- const profile=profiles[0],account=accounts[0],name=profile?.full_name||auth.user.user_metadata?.full_name||auth.user.email?.split('@')[0]||'Trader'
+ const orders=accounts[0]?[]:await getOwnData<Order[]>(`orders?select=id,status,amount,currency,created_at,challenge_plans(name,account_size)&user_id=eq.${auth.user.id}&order=created_at.desc&limit=1`,auth.accessToken)
+ const profile=profiles[0],account=accounts[0],order=orders[0],name=profile?.full_name||auth.user.user_metadata?.full_name||auth.user.email?.split('@')[0]||'Trader'
+ if(!account&&order)return <main className="dash"><Aside/><section><header><div><small>TRADER DASHBOARD</small><h1>Welcome, {name}.</h1></div><LogoutButton/></header><div className="emptyState"><span>PAYMENT REVIEW · {order.status.toUpperCase()}</span><h2>Your payment was submitted.</h2><p>We received your {order.amount} {order.currency} payment notification for {order.challenge_plans?.name||'your challenge'}. Your blockchain transaction is awaiting manual verification.</p><Link className="btn" href="/#plans">View challenge rules →</Link><small>ORDER · {order.id.slice(0,8).toUpperCase()} · KYC {(profile?.kyc_status||'not_started').replace('_',' ').toUpperCase()}</small></div></section></main>
  if(!account)return <main className="dash"><Aside/><section><header><div><small>TRADER DASHBOARD</small><h1>Welcome, {name}.</h1></div><LogoutButton/></header><div className="emptyState"><span>YOUR ACCOUNT IS READY</span><h2>Choose your first challenge.</h2><p>Your secure TakeProp profile is active. Select a package to create your first simulated trading account.</p><Link className="btn" href="/#plans">View challenge plans →</Link><small>KYC STATUS · {(profile?.kyc_status||'not_started').replace('_',' ').toUpperCase()}</small></div></section></main>
  const plan=account.challenge_plans,target=account.starting_balance*Number(plan?.profit_target_pct||0)/100,progress=target?Math.max(0,Math.min(100,account.total_pnl/target*100)):0,dailyFloor=account.starting_balance*(1-Number(plan?.daily_loss_limit_pct||0)/100),maxFloor=account.starting_balance*(1-Number(plan?.max_loss_limit_pct||0)/100)
  return <main className="dash"><Aside/><section><header><div><small>TRADER DASHBOARD</small><h1>Welcome back, {name}.</h1></div><div className="headActions"><button>{account.display_account_id||'TAKEPROP ACCOUNT'}</button><LogoutButton/></div></header><div className="dashCards"><Card l="Balance" v={usd(account.balance)}/><Card l="Equity" v={usd(account.equity)} g/><Card l="Today’s P&L" v={usd(account.daily_pnl)} g={account.daily_pnl>=0}/><Card l="Account status" v={account.status.toUpperCase()} g={account.status==='active'}/></div><div className="dashGrid"><article><header><b>Challenge progress</b><small>{plan?.name||account.phase.toUpperCase()}</small></header><div className="bigProgress"><b>{usd(account.total_pnl)}</b><span> of {usd(target)} target</span><i><b style={{width:`${progress}%`}}/></i><strong>{progress.toFixed(1)}% complete</strong></div><div className="riskRows"><div><span>Daily loss floor</span><b>{usd(dailyFloor)}</b><small>{plan?.daily_loss_limit_pct||0}% daily limit</small></div><div><span>Static loss floor</span><b>{usd(maxFloor)}</b><small>{plan?.max_loss_limit_pct||0}% static limit</small></div></div></article><article><header><b>Account</b><small>SIMULATED</small></header><div className="accountInfo"><span>Starting balance<b>{usd(account.starting_balance)}</b></span><span>Phase<b>{account.phase.toUpperCase()}</b></span><span>KYC status<b>{(profile?.kyc_status||'not_started').replace('_',' ').toUpperCase()}</b></span></div><button className="btn">Open terminal →</button></article></div><div className="disclaimer">All trading activity shown is simulated. Mark price is used for unrealized P&amp;L and risk.</div></section></main>}
diff --git a/apps/web/lib/plans.ts b/apps/web/lib/plans.ts
index 92ac1c7..29206a0 100644
--- a/apps/web/lib/plans.ts
+++ b/apps/web/lib/plans.ts
@@ -2,8 +2,8 @@ export type StepType='ONE_STEP'|'TWO_STEP'
 export type Plan={id:string;balance:number;stepType:StepType;price:number;regularPrice:number;targets:number[];dailyLoss:number;staticLoss:number;rewardSplit:number}
 const data=[[5000,29,49,39,59],[10000,49,69,59,79],[25000,99,139,119,159],[50000,179,229,209,269],[100000,299,399,349,449]]
 export const plans:Plan[]=data.flatMap(([balance,one,oneRegular,two,twoRegular])=>[
- {id:`${balance}-one-step`,balance,stepType:'ONE_STEP',price:one,regularPrice:oneRegular,targets:[8],dailyLoss:3,staticLoss:6,rewardSplit:80},
- {id:`${balance}-two-step`,balance,stepType:'TWO_STEP',price:two,regularPrice:twoRegular,targets:[8,5],dailyLoss:3,staticLoss:8,rewardSplit:90}
+ {id:`one-step-${balance/1000}k`,balance,stepType:'ONE_STEP',price:one,regularPrice:oneRegular,targets:[8],dailyLoss:3,staticLoss:6,rewardSplit:80},
+ {id:`two-step-${balance/1000}k`,balance,stepType:'TWO_STEP',price:two,regularPrice:twoRegular,targets:[8,5],dailyLoss:3,staticLoss:8,rewardSplit:90}
 ])
 export const money=(n:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n)
 export const staticFloor=(n:number,lossPct=6)=>n*(1-lossPct/100)
diff --git a/apps/web/lib/supabase-rest.ts b/apps/web/lib/supabase-rest.ts
index 8bcb903..9e2d0f0 100644
--- a/apps/web/lib/supabase-rest.ts
+++ b/apps/web/lib/supabase-rest.ts
@@ -29,3 +29,16 @@ export async function getOwnData<T>(path: string, accessToken: string): Promise<
   if (!response.ok) throw new Error(`Supabase query failed (${response.status}).`)
   return response.json() as Promise<T>
 }
+
+export async function postOwnData<T>(path: string, accessToken: string, body: unknown): Promise<T> {
+  const { url, key } = supabaseConfig()
+  const response = await fetch(`${url}/rest/v1/${path}`, {
+    method: 'POST',
+    headers: { apikey: key, Authorization: `Bearer ${accessToken}`, 'Content-Type': 'application/json', Prefer: 'return=representation' },
+    body: JSON.stringify(body),
+    cache: 'no-store'
+  })
+  const result = await response.json().catch(() => null)
+  if (!response.ok) throw new Error(result?.message || `Supabase mutation failed (${response.status}).`)
+  return result as T
+}
diff --git a/supabase/migrations/20260817090000_usdt_bep20_order_submission.sql b/supabase/migrations/20260817090000_usdt_bep20_order_submission.sql
new file mode 100644
index 0000000..16092be
--- /dev/null
+++ b/supabase/migrations/20260817090000_usdt_bep20_order_submission.sql
@@ -0,0 +1,28 @@
+drop index if exists public.orders_usdt_tx_unique;
+create unique index orders_usdt_tx_unique
+on public.orders (payment_provider, lower(provider_payment_id))
+where payment_provider in ('usdt_bep20','usdt_erc20','usdt_trc20','usdt_ton') and provider_payment_id is not null;
+
+drop policy if exists orders_insert_own_usdt_pending on public.orders;
+create policy orders_insert_own_usdt_pending
+on public.orders for insert
+to authenticated
+with check (
+  user_id = (select auth.uid())
+  and status = 'pending'
+  and currency = 'USDT'
+  and provider_payment_id ~ '^[A-Za-z0-9+/_=-]{20,100}$'
+  and (
+    (payment_provider = 'usdt_bep20' and provider_checkout_id = '0x30127dee8f4bfeaec586c32d580a8b6066eac11b')
+    or (payment_provider = 'usdt_erc20' and provider_checkout_id = '0x30127dee8f4bfeaec586c32d580a8b6066eac11b')
+    or (payment_provider = 'usdt_trc20' and provider_checkout_id = 'TLTKYRdtpaaYXoSHMgUgEJ3BGARbQkcx3g')
+    or (payment_provider = 'usdt_ton' and provider_checkout_id = 'UQCuT9QECsZp-iBmSM1v8c8Gdga3JWww1sENWn6sywc4cjKc')
+  )
+  and exists (
+    select 1 from public.challenge_plans p
+    where p.id = plan_id and p.is_active = true and p.price = amount and p.version = plan_version
+  )
+);
+
+grant insert (user_id, plan_id, amount, currency, status, payment_provider, provider_checkout_id, provider_payment_id, plan_version, plan_snapshot)
+on public.orders to authenticated;
-- 
2.51.1

