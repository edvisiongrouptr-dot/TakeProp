From a77e21ac9da0bc1d40dc1f5363f280eb0b78774e Mon Sep 17 00:00:00 2001
From: Codex <codex@openai.com>
Date: Mon, 17 Aug 2026 06:05:42 +0200
Subject: [PATCH] Add secure trader authentication and dashboard

---
 .env.example                              |  2 ++
 apps/web/app/api/auth/login/route.ts      | 18 +++++++++++++
 apps/web/app/api/auth/logout/route.ts     | 10 ++++++++
 apps/web/app/api/auth/register/route.ts   | 21 +++++++++++++++
 apps/web/app/auth/auth.css                |  1 +
 apps/web/app/auth/page.tsx                | 21 +++++++++++++++
 apps/web/app/dashboard/auth-dashboard.css |  1 +
 apps/web/app/dashboard/layout.tsx         |  1 +
 apps/web/app/dashboard/logout-button.tsx  |  2 ++
 apps/web/app/dashboard/page.tsx           | 19 +++++++++++++-
 apps/web/app/page.tsx                     |  2 +-
 apps/web/lib/supabase-rest.ts             | 31 +++++++++++++++++++++++
 apps/web/middleware.ts                    | 26 +++++++++++++++++++
 13 files changed, 153 insertions(+), 2 deletions(-)
 create mode 100644 apps/web/app/api/auth/login/route.ts
 create mode 100644 apps/web/app/api/auth/logout/route.ts
 create mode 100644 apps/web/app/api/auth/register/route.ts
 create mode 100644 apps/web/app/auth/auth.css
 create mode 100644 apps/web/app/auth/page.tsx
 create mode 100644 apps/web/app/dashboard/auth-dashboard.css
 create mode 100644 apps/web/app/dashboard/logout-button.tsx
 create mode 100644 apps/web/lib/supabase-rest.ts
 create mode 100644 apps/web/middleware.ts

diff --git a/.env.example b/.env.example
index cbb4d7a..430e5a7 100644
--- a/.env.example
+++ b/.env.example
@@ -2,6 +2,8 @@
 # Copy to .env and fill values before running
 
 DATABASE_URL=postgresql://postgres:postgres@localhost:5432/takeprop?schema=public
+NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
+NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_replace_me
 REDIS_URL=redis://localhost:6379
 JWT_SECRET=replace_me_with_strong_secret
 NODE_ENV=development
diff --git a/apps/web/app/api/auth/login/route.ts b/apps/web/app/api/auth/login/route.ts
new file mode 100644
index 0000000..b7539c4
--- /dev/null
+++ b/apps/web/app/api/auth/login/route.ts
@@ -0,0 +1,18 @@
+import { NextResponse } from 'next/server'
+import { authCookies, supabaseConfig } from '../../../../lib/supabase-rest'
+
+export async function POST(request: Request) {
+  try {
+    const { email, password } = await request.json()
+    if (typeof email !== 'string' || typeof password !== 'string') return NextResponse.json({ error: 'Email and password are required.' }, { status: 400 })
+    const { url, key } = supabaseConfig()
+    const authResponse = await fetch(`${url}/auth/v1/token?grant_type=password`, { method: 'POST', headers: { apikey: key, 'Content-Type': 'application/json' }, body: JSON.stringify({ email: email.trim().toLowerCase(), password }), cache: 'no-store' })
+    const result = await authResponse.json()
+    if (!authResponse.ok) return NextResponse.json({ error: result.msg || result.error_description || 'Unable to sign in.' }, { status: authResponse.status })
+    const response = NextResponse.json({ ok: true })
+    const names = authCookies(); const secure = process.env.NODE_ENV === 'production'
+    response.cookies.set(names.accessCookie, result.access_token, { httpOnly: true, secure, sameSite: 'lax', path: '/', maxAge: result.expires_in || 3600 })
+    response.cookies.set(names.refreshCookie, result.refresh_token, { httpOnly: true, secure, sameSite: 'lax', path: '/', maxAge: 60 * 60 * 24 * 30 })
+    return response
+  } catch { return NextResponse.json({ error: 'Authentication service is not configured.' }, { status: 503 }) }
+}
diff --git a/apps/web/app/api/auth/logout/route.ts b/apps/web/app/api/auth/logout/route.ts
new file mode 100644
index 0000000..df5be04
--- /dev/null
+++ b/apps/web/app/api/auth/logout/route.ts
@@ -0,0 +1,10 @@
+import { NextResponse } from 'next/server'
+import { authCookies, supabaseConfig } from '../../../../lib/supabase-rest'
+
+export async function POST(request: Request) {
+  const names = authCookies(); const token = request.headers.get('cookie')?.match(new RegExp(`${names.accessCookie}=([^;]+)`))?.[1]
+  if (token) { try { const { url, key } = supabaseConfig(); await fetch(`${url}/auth/v1/logout`, { method: 'POST', headers: { apikey: key, Authorization: `Bearer ${token}` }, cache: 'no-store' }) } catch {} }
+  const response = NextResponse.json({ ok: true })
+  response.cookies.set(names.accessCookie, '', { path: '/', maxAge: 0 }); response.cookies.set(names.refreshCookie, '', { path: '/', maxAge: 0 })
+  return response
+}
diff --git a/apps/web/app/api/auth/register/route.ts b/apps/web/app/api/auth/register/route.ts
new file mode 100644
index 0000000..66b553b
--- /dev/null
+++ b/apps/web/app/api/auth/register/route.ts
@@ -0,0 +1,21 @@
+import { NextResponse } from 'next/server'
+import { authCookies, supabaseConfig } from '../../../../lib/supabase-rest'
+
+export async function POST(request: Request) {
+  try {
+    const { fullName, email, password } = await request.json()
+    if (typeof fullName !== 'string' || fullName.trim().length < 2) return NextResponse.json({ error: 'Enter your full name.' }, { status: 400 })
+    if (typeof email !== 'string' || !email.includes('@')) return NextResponse.json({ error: 'Enter a valid email.' }, { status: 400 })
+    if (typeof password !== 'string' || password.length < 8) return NextResponse.json({ error: 'Password must contain at least 8 characters.' }, { status: 400 })
+    const { url, key } = supabaseConfig(); const origin = new URL(request.url).origin
+    const redirectTo=encodeURIComponent(`${origin}/auth?verified=1`)
+    const authResponse = await fetch(`${url}/auth/v1/signup?redirect_to=${redirectTo}`, { method: 'POST', headers: { apikey: key, 'Content-Type': 'application/json' }, body: JSON.stringify({ email: email.trim().toLowerCase(), password, data: { full_name: fullName.trim() } }), cache: 'no-store' })
+    const result = await authResponse.json()
+    if (!authResponse.ok) return NextResponse.json({ error: result.msg || result.error_description || 'Unable to create account.' }, { status: authResponse.status })
+    if (!result.access_token) return NextResponse.json({ ok: true, confirmationRequired: true })
+    const response = NextResponse.json({ ok: true }); const names = authCookies(); const secure = process.env.NODE_ENV === 'production'
+    response.cookies.set(names.accessCookie, result.access_token, { httpOnly: true, secure, sameSite: 'lax', path: '/', maxAge: result.expires_in || 3600 })
+    response.cookies.set(names.refreshCookie, result.refresh_token, { httpOnly: true, secure, sameSite: 'lax', path: '/', maxAge: 60 * 60 * 24 * 30 })
+    return response
+  } catch { return NextResponse.json({ error: 'Authentication service is not configured.' }, { status: 503 }) }
+}
diff --git a/apps/web/app/auth/auth.css b/apps/web/app/auth/auth.css
new file mode 100644
index 0000000..9391043
--- /dev/null
+++ b/apps/web/app/auth/auth.css
@@ -0,0 +1 @@
+.authPage{min-height:100vh;display:grid;grid-template-columns:1fr 1fr;background:#060b0f}.authIntro,.authPanel{padding:54px 7vw}.authIntro{display:flex;flex-direction:column;background:radial-gradient(circle at 20% 70%,#43e28a20,transparent 38%),#081015;border-right:1px solid var(--line)}.authIntro>div{margin:auto 0;max-width:560px}.authIntro span,.authBox>span{color:#76b895;font-size:9px;letter-spacing:2px;font-weight:800}.authIntro h1{font-size:58px;line-height:1.02;letter-spacing:-3px;margin:22px 0}.authIntro p,.authBox>p{color:#7f8e89;line-height:1.7}.authIntro>small{color:#53635d;font-size:9px}.authPanel{display:grid;place-items:center}.authBox{width:min(440px,100%)}.authBox h2{font-size:34px;letter-spacing:-1px;margin:13px 0 5px}.authTabs{display:grid;grid-template-columns:1fr 1fr;background:#0b1317;border:1px solid var(--line);padding:4px;margin:30px 0 24px}.authTabs button{border:0;background:transparent;color:#6e7e78;padding:12px;cursor:pointer}.authTabs .on{background:#14251c;color:var(--green)}.authBox form{display:grid;gap:17px}.authBox label{font-size:8px;letter-spacing:1.4px;color:#6e7d78}.authBox input{display:block;width:100%;margin-top:8px;border:1px solid #26353a;background:#0a1216;color:#f3f7f5;padding:15px;border-radius:3px;outline:none}.authBox input:focus{border-color:#3a9e69}.authSubmit{border:0;cursor:pointer;margin-top:5px}.authSubmit:disabled{opacity:.6}.authMessage{border:1px solid #715b2a;background:#241f10;color:#e5c76e;padding:12px;font-size:11px;line-height:1.5}.authBox>small{display:block;margin-top:20px;color:#53615c;font-size:9px;line-height:1.6}@media(max-width:800px){.authPage{grid-template-columns:1fr}.authIntro{padding:30px 24px;min-height:290px}.authIntro h1{font-size:39px}.authIntro>small{display:none}.authPanel{padding:45px 24px}}
diff --git a/apps/web/app/auth/page.tsx b/apps/web/app/auth/page.tsx
new file mode 100644
index 0000000..c90ef1c
--- /dev/null
+++ b/apps/web/app/auth/page.tsx
@@ -0,0 +1,21 @@
+'use client'
+
+import Link from 'next/link'
+import { FormEvent, useState } from 'react'
+import './auth.css'
+
+export default function AuthPage() {
+  const [mode, setMode] = useState<'login' | 'register'>('login')
+  const [message, setMessage] = useState('')
+  const [loading, setLoading] = useState(false)
+  async function submit(event: FormEvent<HTMLFormElement>) {
+    event.preventDefault(); setLoading(true); setMessage('')
+    const form = new FormData(event.currentTarget)
+    const response = await fetch(`/api/auth/${mode}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ fullName: form.get('fullName'), email: form.get('email'), password: form.get('password') }) })
+    const result = await response.json(); setLoading(false)
+    if (!response.ok) return setMessage(result.error || 'Something went wrong.')
+    if (result.confirmationRequired) return setMessage('Check your email to confirm your account, then sign in.')
+    window.location.href = '/dashboard'
+  }
+  return <main className="authPage"><section className="authIntro"><Link className="brand" href="/"><b>TP</b><strong>TakeProp</strong></Link><div><span>SIMULATED TRADING · SECURE ACCESS</span><h1>Your trading journey starts here.</h1><p>Create your account, choose a challenge and track every rule from one transparent dashboard.</p></div><small>Protected by encrypted, HTTP-only session cookies.</small></section><section className="authPanel"><div className="authBox"><span>TRADER ACCESS</span><h2>{mode === 'login' ? 'Welcome back.' : 'Create your account.'}</h2><p>{mode === 'login' ? 'Sign in to your TakeProp dashboard.' : 'One account for challenges, trading and payouts.'}</p><div className="authTabs"><button className={mode === 'login' ? 'on' : ''} onClick={() => { setMode('login'); setMessage('') }}>Log in</button><button className={mode === 'register' ? 'on' : ''} onClick={() => { setMode('register'); setMessage('') }}>Register</button></div><form onSubmit={submit}>{mode === 'register' && <label>FULL NAME<input name="fullName" autoComplete="name" minLength={2} required placeholder="Your full name" /></label>}<label>EMAIL<input name="email" type="email" autoComplete="email" required placeholder="you@example.com" /></label><label>PASSWORD<input name="password" type="password" autoComplete={mode === 'login' ? 'current-password' : 'new-password'} minLength={8} required placeholder="At least 8 characters" /></label>{message && <div className="authMessage">{message}</div>}<button className="btn authSubmit" disabled={loading}>{loading ? 'Please wait…' : mode === 'login' ? 'Log in →' : 'Create account →'}</button></form><small>By creating an account, you agree to TakeProp’s Terms and Privacy Policy.</small></div></section></main>
+}
diff --git a/apps/web/app/dashboard/auth-dashboard.css b/apps/web/app/dashboard/auth-dashboard.css
new file mode 100644
index 0000000..e563978
--- /dev/null
+++ b/apps/web/app/dashboard/auth-dashboard.css
@@ -0,0 +1 @@
+.headActions{display:flex;gap:8px}.dash>section>header .logout{color:#e4817c;cursor:pointer}.emptyState{max-width:720px;margin:100px auto;background:radial-gradient(circle at 50% 100%,#43e28a1c,transparent 55%),var(--panel);border:1px solid var(--line);padding:70px;text-align:center}.emptyState>span{color:#72ba92;font-size:9px;letter-spacing:2px}.emptyState h2{font-size:38px;letter-spacing:-2px;margin:16px 0}.emptyState p{color:#7b8a85;line-height:1.7}.emptyState .btn{margin:20px 0}.emptyState>small{display:block;color:#53645d;font-size:8px}.accountInfo{display:grid}.accountInfo span{display:flex;justify-content:space-between;padding:17px 0;border-bottom:1px solid var(--line);color:#697873;font-size:10px}.accountInfo b{color:#dce5e1}@media(max-width:800px){.emptyState{margin:50px auto;padding:45px 22px}.emptyState h2{font-size:30px}.headActions>button:first-child{display:none}}
diff --git a/apps/web/app/dashboard/layout.tsx b/apps/web/app/dashboard/layout.tsx
index 4956955..d0a9685 100644
--- a/apps/web/app/dashboard/layout.tsx
+++ b/apps/web/app/dashboard/layout.tsx
@@ -1,2 +1,3 @@
 import './dash.css'
+import './auth-dashboard.css'
 export default function DashboardLayout({children}:{children:React.ReactNode}){return children}
diff --git a/apps/web/app/dashboard/logout-button.tsx b/apps/web/app/dashboard/logout-button.tsx
new file mode 100644
index 0000000..7387d63
--- /dev/null
+++ b/apps/web/app/dashboard/logout-button.tsx
@@ -0,0 +1,2 @@
+'use client'
+export default function LogoutButton(){return <button className="logout" onClick={async()=>{await fetch('/api/auth/logout',{method:'POST'});window.location.href='/'}}>Log out</button>}
diff --git a/apps/web/app/dashboard/page.tsx b/apps/web/app/dashboard/page.tsx
index 99875a9..86e624e 100644
--- a/apps/web/app/dashboard/page.tsx
+++ b/apps/web/app/dashboard/page.tsx
@@ -1,3 +1,20 @@
 import Link from 'next/link'
-export default function Dashboard(){return <main className="dash"><aside><Link className="brand" href="/"><b>TP</b><strong>TakeProp</strong></Link><nav><a className="active">Overview</a><a>Trade</a><a>Orders & positions</a><a>Analytics</a><a>Payouts</a><a>Scaling</a><a>Support</a></nav><small>SIMULATED ENVIRONMENT</small></aside><section><header><div><small>TRADER DASHBOARD</small><h1>Welcome back, Alex.</h1></div><button>TP-25000-01⌄</button></header><div className="dashCards"><Card l="Balance" v="$25,320.40"/><Card l="Equity" v="$25,482.60" g/><Card l="Today’s P&L" v="+$482.60" g/><Card l="Account status" v="ACTIVE" g/></div><div className="dashGrid"><article><header><b>Challenge progress</b><small>ONE-STEP · $25K</small></header><div className="bigProgress"><b>$1,982.60</b><span> of $2,000 target</span><i><b/></i><strong>99.1% complete</strong></div><div className="riskRows"><div><span>Daily loss remaining</span><b>$750.00</b><small>Floor $24,250.00</small></div><div><span>Static loss remaining</span><b>$1,500.00</b><small>Floor $23,500.00</small></div></div></article><article><header><b>Markets</b><small>MARK PRICE</small></header>{[['BTCUSDT','$64,286.40','+2.84%'],['ETHUSDT','$3,482.12','+1.62%'],['SOLUSDT','$148.24','-0.41%']].map(x=><div className="marketRow" key={x[0]}><b>{x[0]}</b><span>{x[1]}</span><em className={x[2][0]==='-'?'red':'green'}>{x[2]}</em></div>)}<button className="btn">Open terminal →</button></article></div><div className="disclaimer">All trading activity shown is simulated. Mark price is used for unrealized P&amp;L and risk.</div></section></main>}
+import { redirect } from 'next/navigation'
+import { getAuthenticatedUser, getOwnData } from '../../lib/supabase-rest'
+import LogoutButton from './logout-button'
+
+type Profile={full_name:string|null;kyc_status:string}
+type Plan={name:string;profit_target_pct:number;daily_loss_limit_pct:number;max_loss_limit_pct:number}
+type Account={display_account_id:string|null;status:string;phase:string;starting_balance:number;balance:number;equity:number;daily_pnl:number;total_pnl:number;challenge_plans:Plan|null}
+const usd=(value:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(value)
+
+export default async function Dashboard(){
+ const auth=await getAuthenticatedUser(); if(!auth) redirect('/auth')
+ const profiles=await getOwnData<Profile[]>(`profiles?select=full_name,kyc_status&id=eq.${auth.user.id}&limit=1`,auth.accessToken)
+ const accounts=await getOwnData<Account[]>(`trading_accounts?select=display_account_id,status,phase,starting_balance,balance,equity,daily_pnl,total_pnl,challenge_plans(name,profit_target_pct,daily_loss_limit_pct,max_loss_limit_pct)&user_id=eq.${auth.user.id}&order=created_at.desc&limit=1`,auth.accessToken)
+ const profile=profiles[0],account=accounts[0],name=profile?.full_name||auth.user.user_metadata?.full_name||auth.user.email?.split('@')[0]||'Trader'
+ if(!account)return <main className="dash"><Aside/><section><header><div><small>TRADER DASHBOARD</small><h1>Welcome, {name}.</h1></div><LogoutButton/></header><div className="emptyState"><span>YOUR ACCOUNT IS READY</span><h2>Choose your first challenge.</h2><p>Your secure TakeProp profile is active. Select a package to create your first simulated trading account.</p><Link className="btn" href="/#plans">View challenge plans →</Link><small>KYC STATUS · {(profile?.kyc_status||'not_started').replace('_',' ').toUpperCase()}</small></div></section></main>
+ const plan=account.challenge_plans,target=account.starting_balance*Number(plan?.profit_target_pct||0)/100,progress=target?Math.max(0,Math.min(100,account.total_pnl/target*100)):0,dailyFloor=account.starting_balance*(1-Number(plan?.daily_loss_limit_pct||0)/100),maxFloor=account.starting_balance*(1-Number(plan?.max_loss_limit_pct||0)/100)
+ return <main className="dash"><Aside/><section><header><div><small>TRADER DASHBOARD</small><h1>Welcome back, {name}.</h1></div><div className="headActions"><button>{account.display_account_id||'TAKEPROP ACCOUNT'}</button><LogoutButton/></div></header><div className="dashCards"><Card l="Balance" v={usd(account.balance)}/><Card l="Equity" v={usd(account.equity)} g/><Card l="Today’s P&L" v={usd(account.daily_pnl)} g={account.daily_pnl>=0}/><Card l="Account status" v={account.status.toUpperCase()} g={account.status==='active'}/></div><div className="dashGrid"><article><header><b>Challenge progress</b><small>{plan?.name||account.phase.toUpperCase()}</small></header><div className="bigProgress"><b>{usd(account.total_pnl)}</b><span> of {usd(target)} target</span><i><b style={{width:`${progress}%`}}/></i><strong>{progress.toFixed(1)}% complete</strong></div><div className="riskRows"><div><span>Daily loss floor</span><b>{usd(dailyFloor)}</b><small>{plan?.daily_loss_limit_pct||0}% daily limit</small></div><div><span>Static loss floor</span><b>{usd(maxFloor)}</b><small>{plan?.max_loss_limit_pct||0}% static limit</small></div></div></article><article><header><b>Account</b><small>SIMULATED</small></header><div className="accountInfo"><span>Starting balance<b>{usd(account.starting_balance)}</b></span><span>Phase<b>{account.phase.toUpperCase()}</b></span><span>KYC status<b>{(profile?.kyc_status||'not_started').replace('_',' ').toUpperCase()}</b></span></div><button className="btn">Open terminal →</button></article></div><div className="disclaimer">All trading activity shown is simulated. Mark price is used for unrealized P&amp;L and risk.</div></section></main>}
+function Aside(){return <aside><Link className="brand" href="/"><b>TP</b><strong>TakeProp</strong></Link><nav><a className="active">Overview</a><a>Trade</a><a>Orders &amp; positions</a><a>Analytics</a><a>Payouts</a><a>Scaling</a><a>Support</a></nav><small>SIMULATED ENVIRONMENT</small></aside>}
 function Card({l,v,g}:{l:string,v:string,g?:boolean}){return <article><small>{l}</small><b className={g?'green':''}>{v}</b></article>}
diff --git a/apps/web/app/page.tsx b/apps/web/app/page.tsx
index 5018f08..6d1025a 100644
--- a/apps/web/app/page.tsx
+++ b/apps/web/app/page.tsx
@@ -4,7 +4,7 @@ import {money,plans,StepType} from '../lib/plans'
 const sizes=[5000,10000,25000,50000,100000]
 export default function Home(){const[step,setStep]=useState<StepType>('ONE_STEP');const[size,setSize]=useState(5000);const plan=useMemo(()=>plans.find(p=>p.balance===size&&p.stepType===step)!,[size,step]);return <main>
  <div className="notice">SIMULATED TRADING <i/> UP TO 90% PERFORMANCE REWARDS <i/> STATIC DRAWDOWN</div>
- <header className="nav shell"><a className="brand" href="#top"><b>TP</b><strong>TakeProp</strong></a><nav><a href="#how">How it works</a><a href="#plans">Challenges</a><a href="#rules">Rules</a><a href="#faq">FAQ</a></nav><div><a href="/dashboard">Log in</a><a className="btn small" href="#plans">Get funded</a></div></header>
+ <header className="nav shell"><a className="brand" href="#top"><b>TP</b><strong>TakeProp</strong></a><nav><a href="#how">How it works</a><a href="#plans">Challenges</a><a href="#rules">Rules</a><a href="#faq">FAQ</a></nav><div><a href="/auth">Log in</a><a className="btn small" href="#plans">Get funded</a></div></header>
  <section className="hero shell" id="top"><div><span className="eyebrow">● BUILT FOR DISCIPLINED CRYPTO TRADERS</span><h1>Your edge.<br/><em>Our capital.</em></h1><p>Prove your strategy in a transparent simulated evaluation. Keep up to 90% of your performance rewards—with no hidden rules, moving drawdown, or time pressure.</p><div className="actions"><a className="btn" href="#plans">Start from $29 →</a><a href="#how">See how it works</a></div><small>✓ No time limit　 ✓ Static 6–8% max loss　 ✓ 80–90% reward split</small></div><Terminal/></section>
  <section className="metrics"><div className="shell"><Metric a="UP TO 90%" b="REWARD SPLIT"/><Metric a="10×" b="MAX LEVERAGE"/><Metric a="0" b="MINIMUM DAYS"/><Metric a="24/7" b="CRYPTO MARKETS"/></div></section>
  <section className="section shell" id="how"><Title over="THE PROCESS" title={<>Three steps to <em>funded.</em></>}/><div className="grid three"><Card n="01" title="Choose your challenge">Select an account size and one-step or two-step evaluation.</Card><Card n="02" title="Prove your edge">Reach the target within fixed, transparent risk limits.</Card><Card n="03" title="Get funded">Complete review and unlock a fresh simulated funded account.</Card></div></section>
diff --git a/apps/web/lib/supabase-rest.ts b/apps/web/lib/supabase-rest.ts
new file mode 100644
index 0000000..8bcb903
--- /dev/null
+++ b/apps/web/lib/supabase-rest.ts
@@ -0,0 +1,31 @@
+import { cookies } from 'next/headers'
+
+const accessCookie = 'takeprop-access-token'
+const refreshCookie = 'takeprop-refresh-token'
+
+export type AuthUser = { id: string; email?: string; user_metadata?: { full_name?: string } }
+
+export function supabaseConfig() {
+  const url = process.env.NEXT_PUBLIC_SUPABASE_URL
+  const key = process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
+  if (!url || !key) throw new Error('Supabase environment variables are missing.')
+  return { url: url.replace(/\/$/, ''), key }
+}
+
+export function authCookies() { return { accessCookie, refreshCookie } }
+
+export async function getAuthenticatedUser(): Promise<{ user: AuthUser; accessToken: string } | null> {
+  const accessToken = cookies().get(accessCookie)?.value
+  if (!accessToken) return null
+  const { url, key } = supabaseConfig()
+  const response = await fetch(`${url}/auth/v1/user`, { headers: { apikey: key, Authorization: `Bearer ${accessToken}` }, cache: 'no-store' })
+  if (!response.ok) return null
+  return { user: (await response.json()) as AuthUser, accessToken }
+}
+
+export async function getOwnData<T>(path: string, accessToken: string): Promise<T> {
+  const { url, key } = supabaseConfig()
+  const response = await fetch(`${url}/rest/v1/${path}`, { headers: { apikey: key, Authorization: `Bearer ${accessToken}` }, cache: 'no-store' })
+  if (!response.ok) throw new Error(`Supabase query failed (${response.status}).`)
+  return response.json() as Promise<T>
+}
diff --git a/apps/web/middleware.ts b/apps/web/middleware.ts
new file mode 100644
index 0000000..39a0ae4
--- /dev/null
+++ b/apps/web/middleware.ts
@@ -0,0 +1,26 @@
+import { NextRequest, NextResponse } from 'next/server'
+
+const accessName='takeprop-access-token',refreshName='takeprop-refresh-token'
+
+export async function middleware(request:NextRequest){
+ const access=request.cookies.get(accessName)?.value,refresh=request.cookies.get(refreshName)?.value
+ const url=process.env.NEXT_PUBLIC_SUPABASE_URL?.replace(/\/$/,'')
+ const key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
+ if(!url||!key)return NextResponse.redirect(new URL('/auth?error=config',request.url))
+ if(access){
+  const check=await fetch(`${url}/auth/v1/user`,{headers:{apikey:key,Authorization:`Bearer ${access}`},cache:'no-store'})
+  if(check.ok)return NextResponse.next()
+ }
+ if(refresh){
+  const renewed=await fetch(`${url}/auth/v1/token?grant_type=refresh_token`,{method:'POST',headers:{apikey:key,'Content-Type':'application/json'},body:JSON.stringify({refresh_token:refresh}),cache:'no-store'})
+  if(renewed.ok){
+   const tokens=await renewed.json(),response=NextResponse.redirect(request.nextUrl),secure=process.env.NODE_ENV==='production'
+   response.cookies.set(accessName,tokens.access_token,{httpOnly:true,secure,sameSite:'lax',path:'/',maxAge:tokens.expires_in||3600})
+   response.cookies.set(refreshName,tokens.refresh_token,{httpOnly:true,secure,sameSite:'lax',path:'/',maxAge:60*60*24*30})
+   return response
+  }
+ }
+ const response=NextResponse.redirect(new URL('/auth',request.url));response.cookies.delete(accessName);response.cookies.delete(refreshName);return response
+}
+
+export const config={matcher:['/dashboard/:path*']}
-- 
2.51.1

