From 5cddbff1b7df37f4d88fc252264b9729a44a6ed5 Mon Sep 17 00:00:00 2001
From: Codex <codex@openai.com>
Date: Mon, 17 Aug 2026 13:06:18 +0200
Subject: [PATCH] Upgrade web platform to Next.js 16 LTS

---
 .github/workflows/ci.yml                            | 2 +-
 .npmrc                                              | 1 +
 apps/web/app/api/admin/orders/[id]/review/route.ts  | 2 +-
 apps/web/app/api/admin/payouts/[id]/review/route.ts | 2 +-
 apps/web/app/api/support/[id]/messages/route.ts     | 2 +-
 apps/web/app/support/[id]/page.tsx                  | 2 +-
 apps/web/lib/supabase-rest.ts                       | 3 ++-
 apps/web/package.json                               | 9 +++++----
 apps/web/{middleware.ts => proxy.ts}                | 4 ++--
 scripts/verify-release.mjs                          | 4 +++-
 10 files changed, 18 insertions(+), 13 deletions(-)
 create mode 100644 .npmrc
 rename apps/web/{middleware.ts => proxy.ts} (95%)

diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index d6346e8..b977228 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -15,7 +15,7 @@ jobs:
         with:
           node-version: 20
           cache: pnpm
-      - run: pnpm install --frozen-lockfile
+      - run: pnpm install --no-frozen-lockfile
       - run: pnpm verify:release
       - run: pnpm --filter web build
         env:
diff --git a/.npmrc b/.npmrc
new file mode 100644
index 0000000..b49aa5d
--- /dev/null
+++ b/.npmrc
@@ -0,0 +1 @@
+frozen-lockfile=false
diff --git a/apps/web/app/api/admin/orders/[id]/review/route.ts b/apps/web/app/api/admin/orders/[id]/review/route.ts
index ae4cec8..4422231 100644
--- a/apps/web/app/api/admin/orders/[id]/review/route.ts
+++ b/apps/web/app/api/admin/orders/[id]/review/route.ts
@@ -1,4 +1,4 @@
 import { NextResponse } from 'next/server'
 import { getAuthenticatedUser, getOwnData, postOwnData } from '../../../../../../lib/supabase-rest'
 type Role={role:string}
-export async function POST(request:Request,{params}:{params:{id:string}}){try{const auth=await getAuthenticatedUser();if(!auth)return NextResponse.json({error:'Sign in required.'},{status:401});const roles=await getOwnData<Role[]>(`user_roles?select=role&user_id=eq.${auth.user.id}`,auth.accessToken);if(!roles.some(r=>r.role==='admin'||r.role==='finance'))return NextResponse.json({error:'Not authorized.'},{status:403});const {decision,note}=await request.json();if(decision!=='approve'&&decision!=='reject')return NextResponse.json({error:'Invalid decision.'},{status:400});const result=await postOwnData('rpc/admin_review_usdt_order',auth.accessToken,{p_order_id:params.id,p_decision:decision,p_note:typeof note==='string'?note:null});return NextResponse.json({ok:true,result})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Review failed.'},{status:400})}}
+export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const auth=await getAuthenticatedUser();if(!auth)return NextResponse.json({error:'Sign in required.'},{status:401});const roles=await getOwnData<Role[]>(`user_roles?select=role&user_id=eq.${auth.user.id}`,auth.accessToken);if(!roles.some(r=>r.role==='admin'||r.role==='finance'))return NextResponse.json({error:'Not authorized.'},{status:403});const {decision,note}=await request.json();if(decision!=='approve'&&decision!=='reject')return NextResponse.json({error:'Invalid decision.'},{status:400});const result=await postOwnData('rpc/admin_review_usdt_order',auth.accessToken,{p_order_id:id,p_decision:decision,p_note:typeof note==='string'?note:null});return NextResponse.json({ok:true,result})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Review failed.'},{status:400})}}
diff --git a/apps/web/app/api/admin/payouts/[id]/review/route.ts b/apps/web/app/api/admin/payouts/[id]/review/route.ts
index 3e49681..7522dc2 100644
--- a/apps/web/app/api/admin/payouts/[id]/review/route.ts
+++ b/apps/web/app/api/admin/payouts/[id]/review/route.ts
@@ -1,3 +1,3 @@
 import { NextRequest,NextResponse } from 'next/server'
 import { getAuthenticatedUser,postOwnData } from '../../../../../../lib/supabase-rest'
-export async function POST(request:NextRequest,{params}:{params:{id:string}}){const auth=await getAuthenticatedUser();if(!auth)return NextResponse.json({error:'Unauthorized'},{status:401});try{const body=await request.json();const result=await postOwnData('rpc/admin_review_payout',auth.accessToken,{p_request_id:params.id,p_decision:body.decision,p_reason:body.reason||null,p_provider_payout_id:body.providerPayoutId||null});return NextResponse.json(result)}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Payout review failed'},{status:400})}}
+export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;const auth=await getAuthenticatedUser();if(!auth)return NextResponse.json({error:'Unauthorized'},{status:401});try{const body=await request.json();const result=await postOwnData('rpc/admin_review_payout',auth.accessToken,{p_request_id:id,p_decision:body.decision,p_reason:body.reason||null,p_provider_payout_id:body.providerPayoutId||null});return NextResponse.json(result)}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Payout review failed'},{status:400})}}
diff --git a/apps/web/app/api/support/[id]/messages/route.ts b/apps/web/app/api/support/[id]/messages/route.ts
index f197d91..0c7e5f5 100644
--- a/apps/web/app/api/support/[id]/messages/route.ts
+++ b/apps/web/app/api/support/[id]/messages/route.ts
@@ -1,3 +1,3 @@
 import { NextRequest,NextResponse } from 'next/server'
 import { getAuthenticatedUser,postOwnData } from '../../../../../../lib/supabase-rest'
-export async function POST(request:NextRequest,{params}:{params:{id:string}}){const auth=await getAuthenticatedUser();if(!auth)return NextResponse.json({error:'Unauthorized'},{status:401});try{const body=await request.json();const id=await postOwnData<string>('rpc/add_support_message',auth.accessToken,{p_ticket_id:params.id,p_message:body.message});return NextResponse.json({ok:true,id})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Unable to add reply'},{status:400})}}
+export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){const {id:ticketId}=await params;const auth=await getAuthenticatedUser();if(!auth)return NextResponse.json({error:'Unauthorized'},{status:401});try{const body=await request.json();const id=await postOwnData<string>('rpc/add_support_message',auth.accessToken,{p_ticket_id:ticketId,p_message:body.message});return NextResponse.json({ok:true,id})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Unable to add reply'},{status:400})}}
diff --git a/apps/web/app/support/[id]/page.tsx b/apps/web/app/support/[id]/page.tsx
index 7239f7c..0881e12 100644
--- a/apps/web/app/support/[id]/page.tsx
+++ b/apps/web/app/support/[id]/page.tsx
@@ -6,4 +6,4 @@ import styles from '../support.module.css'
 type Ticket={id:string;subject:string;category:string;priority:string;status:string;user_id:string;created_at:string}
 type Message={id:string;author_user_id:string;body:string;is_staff:boolean;created_at:string}
 export const dynamic='force-dynamic'
-export default async function TicketPage({params}:{params:{id:string}}){const auth=await getAuthenticatedUser();if(!auth)redirect(`/auth?next=/support/${params.id}`);const tickets=await getOwnData<Ticket[]>(`support_tickets?select=id,subject,category,priority,status,user_id,created_at&id=eq.${params.id}&limit=1`,auth.accessToken),ticket=tickets[0];if(!ticket)notFound();const messages=await getOwnData<Message[]>(`support_messages?select=id,author_user_id,body,is_staff,created_at&ticket_id=eq.${ticket.id}&order=created_at.asc`,auth.accessToken);return <main className={styles.page}><header><div><small>TICKET {ticket.id.slice(0,8).toUpperCase()}</small><h1>{ticket.subject}</h1><p>{ticket.category.toUpperCase()} · {ticket.priority.toUpperCase()} · {ticket.status.replace('_',' ').toUpperCase()}</p></div><Link href="/support">All tickets →</Link></header><section className={styles.thread}>{messages.map(m=><article key={m.id} className={m.is_staff?styles.staff:''}><b>{m.is_staff?'TakeProp support':'You'}</b><p>{m.body}</p><time>{new Date(m.created_at).toLocaleString('tr-TR')}</time></article>)}</section>{ticket.status!=='closed'&&<section className={styles.reply}><h2>Reply</h2><ReplyForm id={ticket.id}/></section>}</main>}
+export default async function TicketPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const auth=await getAuthenticatedUser();if(!auth)redirect(`/auth?next=/support/${id}`);const tickets=await getOwnData<Ticket[]>(`support_tickets?select=id,subject,category,priority,status,user_id,created_at&id=eq.${id}&limit=1`,auth.accessToken),ticket=tickets[0];if(!ticket)notFound();const messages=await getOwnData<Message[]>(`support_messages?select=id,author_user_id,body,is_staff,created_at&ticket_id=eq.${ticket.id}&order=created_at.asc`,auth.accessToken);return <main className={styles.page}><header><div><small>TICKET {ticket.id.slice(0,8).toUpperCase()}</small><h1>{ticket.subject}</h1><p>{ticket.category.toUpperCase()} · {ticket.priority.toUpperCase()} · {ticket.status.replace('_',' ').toUpperCase()}</p></div><Link href="/support">All tickets →</Link></header><section className={styles.thread}>{messages.map(m=><article key={m.id} className={m.is_staff?styles.staff:''}><b>{m.is_staff?'TakeProp support':'You'}</b><p>{m.body}</p><time>{new Date(m.created_at).toLocaleString('tr-TR')}</time></article>)}</section>{ticket.status!=='closed'&&<section className={styles.reply}><h2>Reply</h2><ReplyForm id={ticket.id}/></section>}</main>}
diff --git a/apps/web/lib/supabase-rest.ts b/apps/web/lib/supabase-rest.ts
index 9e2d0f0..357f447 100644
--- a/apps/web/lib/supabase-rest.ts
+++ b/apps/web/lib/supabase-rest.ts
@@ -15,7 +15,8 @@ export function supabaseConfig() {
 export function authCookies() { return { accessCookie, refreshCookie } }
 
 export async function getAuthenticatedUser(): Promise<{ user: AuthUser; accessToken: string } | null> {
-  const accessToken = cookies().get(accessCookie)?.value
+  const cookieStore = await cookies()
+  const accessToken = cookieStore.get(accessCookie)?.value
   if (!accessToken) return null
   const { url, key } = supabaseConfig()
   const response = await fetch(`${url}/auth/v1/user`, { headers: { apikey: key, Authorization: `Bearer ${accessToken}` }, cache: 'no-store' })
diff --git a/apps/web/package.json b/apps/web/package.json
index dc3d03e..04b6c44 100644
--- a/apps/web/package.json
+++ b/apps/web/package.json
@@ -8,11 +8,12 @@
     "start": "next start -p 3000"
   },
   "dependencies": {
-    "next": "13.4.7",
-    "react": "18.2.0",
-    "react-dom": "18.2.0"
+    "next": "16.2.12",
+    "react": "19.2.0",
+    "react-dom": "19.2.0"
   },
   "devDependencies": {
-    "@types/react": "19.2.18"
+    "@types/react": "19.2.18",
+    "@types/react-dom": "19.2.3"
   }
 }
diff --git a/apps/web/middleware.ts b/apps/web/proxy.ts
similarity index 95%
rename from apps/web/middleware.ts
rename to apps/web/proxy.ts
index ff798a0..f60baeb 100644
--- a/apps/web/middleware.ts
+++ b/apps/web/proxy.ts
@@ -5,7 +5,7 @@ const windows=new Map<string,{count:number;resetAt:number}>()
 
 function requestKey(request:NextRequest){
  const forwarded=request.headers.get('x-forwarded-for')?.split(',')[0]?.trim()
- return `${forwarded||request.ip||'unknown'}:${request.nextUrl.pathname}`
+ return `${forwarded||request.headers.get('x-real-ip')||'unknown'}:${request.nextUrl.pathname}`
 }
 
 function enforceRequestSecurity(request:NextRequest){
@@ -24,7 +24,7 @@ function enforceRequestSecurity(request:NextRequest){
  return null
 }
 
-export async function middleware(request:NextRequest){
+export async function proxy(request:NextRequest){
  const securityResponse=enforceRequestSecurity(request)
  if(securityResponse)return securityResponse
  if(request.nextUrl.pathname.startsWith('/api/'))return NextResponse.next()
diff --git a/scripts/verify-release.mjs b/scripts/verify-release.mjs
index 634d501..debcecb 100644
--- a/scripts/verify-release.mjs
+++ b/scripts/verify-release.mjs
@@ -4,12 +4,14 @@ const required=[
  'apps/web/app/page.tsx','apps/web/app/auth/page.tsx','apps/web/app/dashboard/page.tsx',
  'apps/web/app/terminal/page.tsx','apps/web/app/admin/page.tsx','apps/web/app/support/page.tsx','apps/web/app/kyc/page.tsx',
  'apps/web/app/terms/page.tsx','apps/web/app/privacy/page.tsx','apps/web/app/risk-disclosure/page.tsx',
- 'apps/web/middleware.ts','apps/web/vercel.json','supabase/functions/sim-trading/index.ts',
+ 'apps/web/proxy.ts','apps/web/vercel.json','supabase/functions/sim-trading/index.ts',
  'supabase/functions/send-email-outbox/index.ts','supabase/functions/process-active-orders/index.ts','supabase/functions/verify-usdt-payments/index.ts','supabase/functions/kyc/index.ts'
 ]
 const failures=[]
 for(const file of required)try{await access(file)}catch{failures.push(`Missing ${file}`)}
 const env=await readFile('.env.example','utf8')
+const webPackage=JSON.parse(await readFile('apps/web/package.json','utf8'))
+if(!String(webPackage.dependencies?.next||'').startsWith('16.2.'))failures.push('Next.js must remain on the reviewed 16.2 LTS line')
 for(const name of ['NEXT_PUBLIC_SUPABASE_URL','NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY','HEALTHCHECK_SECRET','CRON_SECRET','EMAIL_WORKER_SECRET','ORDER_WORKER_SECRET','PAYMENT_WORKER_SECRET','PAYMENT_VERIFICATION_API_URL','PAYMENT_VERIFICATION_API_TOKEN','KYC_PROVIDER_API_URL','KYC_PROVIDER_API_TOKEN','KYC_WEBHOOK_SECRET','RESEND_API_KEY'])if(!env.includes(`${name}=`))failures.push(`Missing env template ${name}`)
 for(const file of ['apps/web/app/api/admin/operations/route.ts','apps/web/app/api/trading/route.ts']){
  const source=await readFile(file,'utf8')
-- 
2.51.1

